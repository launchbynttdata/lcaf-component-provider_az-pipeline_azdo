parameters:
  - name: legacy-peer-deps
    type: boolean
    default: false

steps:
  - task: CmdLine@2
    displayName: npm install
    inputs:
      script: |
        npm config set legacy-peer-deps ${{ parameters.legacy-peer-deps }}
        npm install