steps:
  - task: AzureClI@2
    displayName: Launch Make Configure
    inputs:
      scriptType: bash
      scriptLocation: inlineScript
      workingDirectory: $(System.DefaultWorkingDirectory)
      inlineScript: |
        make configure
      azureSubscription: ${{ parameters.serviceConnection }}
      addSpnToEnvironment: true